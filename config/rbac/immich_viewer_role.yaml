# permissions for end users to view immiches.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: immich-operator
    app.kubernetes.io/managed-by: kustomize
  name: immich-viewer-role
rules:
- apiGroups:
  - media.rm3l.org
  resources:
  - immiches
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - media.rm3l.org
  resources:
  - immiches/status
  verbs:
  - get
